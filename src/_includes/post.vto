<article class="post wrap">
  <header class="post-header">
    <p class="eyebrow">Writing</p>
    <h1>{{ title }}</h1>
    {{ if description }}<p class="post-dek">{{ description }}</p>{{ /if }}
  </header>
  <div class="post-content">{{ content }}</div>
</article>
